<h1>Connecte toi mon p'tit loulou:</h1>

<!-- avec FORM TAG -->

  <%= form_tag url_for(action: 'create'), method: "post" do %>

    <!-- ci-dessous = authenticity token (cf. cours THP du Mardi semaine) -->      
      <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">    

        <%= label_tag 'Ton Adresse mail :' %>
        <%= text_field_tag 'email' %>   
        <br /> <br />

        <%= label_tag 'Ton mot de passe :' %> <!-- rajouter un "hidden" quelquepart pour masquer quand on tape son MdP -->
        <%= text_area_tag 'password' %> 
        <br /> <br />

        <%= submit_tag "C'est dans la boite !" %>     

        <% end %>

<!-- comment mettre un "if xxx.errors.any?" pour faire des vérifications puisqu'ici on ne fait pas appel à un Model 'Session' ???